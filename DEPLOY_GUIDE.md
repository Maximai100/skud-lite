# 📚 Подробная инструкция по деплою СКУД-лайт

Эта инструкция написана максимально подробно — шаг за шагом, как для школьника.

**Всё будет работать в облаке 24/7 — ваш компьютер можно выключить!**

---

## 📋 Содержание

1. [Что нам понадобится](#1-что-нам-понадобится)
2. [Подготовка проекта](#2-подготовка-проекта)
3. [Деплой Backend на Render](#3-деплой-backend-на-render)
4. [Настройка Telegram-бота](#4-настройка-telegram-бота)
5. [Деплой бота на Render](#5-деплой-бота-на-render)
6. [Создание QR-кода](#6-создание-qr-кода)
7. [Как пользоваться системой](#7-как-пользоваться-системой)

---

## 1. Что нам понадобится

### Аккаунты (всё бесплатно):
- **GitHub** — для хранения кода: https://github.com
- **Render** — для запуска сервера и бота: https://render.com
- **Telegram** — для бота

### Программы на компьютере (только для подготовки):
- Любой текстовый редактор (Блокнот, VS Code)
- Браузер

> 💡 После настройки ваш компьютер можно выключить — всё будет работать в облаке!

---

## 2. Подготовка проекта

### Шаг 2.1: Создаём аккаунт на GitHub

1. Откройте https://github.com
2. Нажмите **Sign up**
3. Введите email, пароль, имя пользователя
4. Подтвердите email (придёт письмо)

### Шаг 2.2: Создаём репозиторий

1. На GitHub нажмите зелёную кнопку **New** (или плюсик → New repository)
2. Заполните:
   - **Repository name**: `skud-lite`
   - **Description**: `Система учёта присутствия`
   - Выберите **Public**
   - ✅ Поставьте галочку **Add a README file**
3. Нажмите **Create repository**

### Шаг 2.3: Загружаем код в репозиторий

1. Откройте ваш репозиторий на GitHub
2. Нажмите **Add file** → **Upload files**
3. Перетащите папки `backend`, `frontend`, `bot` и файлы `README.md`, `DEPLOY_GUIDE.md`
4. Внизу нажмите **Commit changes**

**Структура должна выглядеть так:**
```
skud-lite/
├── backend/
│   ├── main.py
│   ├── models.py
│   ├── database.py
│   └── requirements.txt
├── frontend/
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── bot/
│   ├── bot.py
│   └── requirements.txt
└── README.md
```

---

## 3. Деплой Backend на Render

### Почему Render, а не Vercel?

Vercel не поддерживает Python + SQLite. Render — бесплатный и простой.

### Шаг 3.1: Регистрация на Render

1. Откройте https://render.com
2. Нажмите **Get Started for Free**
3. Выберите **Sign up with GitHub** (проще всего!)
4. Разрешите доступ к вашему GitHub

### Шаг 3.2: Создаём Web Service для Backend

1. На главной странице Render нажмите **New** → **Web Service**
2. Выберите **Build and deploy from a Git repository** → **Next**
3. Найдите ваш репозиторий `skud-lite` и нажмите **Connect**

### Шаг 3.3: Настройка Backend

Заполните поля:

| Поле | Значение |
|------|----------|
| **Name** | `skud-lite-backend` |
| **Region** | Frankfurt (EU Central) |
| **Branch** | `main` |
| **Root Directory** | `backend` |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `uvicorn main:app --host 0.0.0.0 --port $PORT` |

### Шаг 3.4: Выбираем план

1. Прокрутите вниз до **Instance Type**
2. Выберите **Free** (бесплатный)
3. Нажмите **Create Web Service**

### Шаг 3.5: Ждём деплой

1. Render начнёт сборку — это займёт 2-5 минут
2. Когда статус станет **Live** — готово!

### Шаг 3.6: Сохраняем URL

1. Вверху страницы будет ваш URL, например:
   ```
   https://skud-lite-backend.onrender.com
   ```
2. **Скопируйте и сохраните этот URL!** Он понадобится для бота.

### Шаг 3.7: Проверяем работу

1. Откройте в браузере ваш URL
2. Должен открыться веб-интерфейс для жильцов
3. Попробуйте зарегистрироваться — введите ФИО

> ⚠️ **Важно**: На бесплатном плане сервер "засыпает" через 15 минут. Первый запрос после сна занимает 30-60 секунд.

---

## 4. Настройка Telegram-бота

### Шаг 4.1: Создаём бота в Telegram

1. Откройте Telegram на телефоне или компьютере
2. В поиске найдите **@BotFather** (с синей галочкой!)
3. Напишите ему: `/newbot`
4. Ответьте на вопросы:
   - **Имя бота**: `СКУД Дежурный` (любое на русском)
   - **Username**: `skud_duty_bot` (латиницей, заканчивается на `_bot`)
5. BotFather пришлёт **токен**:
   ```
   7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
   ```
6. **Скопируйте токен!** Никому не показывайте!

### Шаг 4.2: Узнаём свой Telegram ID

1. В Telegram найдите бота **@userinfobot**
2. Напишите ему `/start`
3. Он пришлёт ваш ID: `123456789`
4. **Сохраните этот ID!**

> 💡 Если дежурных несколько — соберите ID у каждого.

---

## 5. Деплой бота на Render

**Бот тоже будет работать в облаке 24/7!**

### Шаг 5.1: Создаём Background Worker

1. На Render нажмите **New** → **Background Worker**
2. Выберите репозиторий `skud-lite` → **Connect**

### Шаг 5.2: Настройка бота

| Поле | Значение |
|------|----------|
| **Name** | `skud-lite-bot` |
| **Region** | Frankfurt (EU Central) |
| **Branch** | `main` |
| **Root Directory** | `bot` |
| **Runtime** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python bot.py` |

### Шаг 5.3: Добавляем переменные окружения

⚠️ **Это важный шаг!** Без него бот не заработает.

1. Прокрутите вниз до раздела **Environment Variables**
2. Нажмите **Add Environment Variable**
3. Добавьте три переменные:

| Key | Value (ваши данные!) |
|-----|----------------------|
| `BOT_TOKEN` | `7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw` |
| `API_URL` | `https://skud-lite-backend.onrender.com` |
| `ADMIN_IDS` | `123456789` |

**Где взять значения:**
- `BOT_TOKEN` — из шага 4.1 (от BotFather)
- `API_URL` — из шага 3.6 (URL вашего backend)
- `ADMIN_IDS` — из шага 4.2 (ваш Telegram ID)

> 💡 Если дежурных несколько: `ADMIN_IDS` = `123456789,987654321,111222333`

### Шаг 5.4: Создаём сервис

1. Выберите **Free** план
2. Нажмите **Create Background Worker**

### Шаг 5.5: Проверяем работу

1. Подождите 2-3 минуты пока бот запустится
2. В логах (Logs) должно появиться: `Бот запущен...`
3. Откройте Telegram, найдите вашего бота
4. Напишите `/start`
5. Должно появиться приветствие с кнопками!

---

## 6. Создание QR-кода

### Шаг 6.1: Генерация

1. Откройте https://www.qrcode-monkey.com/ (бесплатный)
2. В поле **Your URL** введите:
   ```
   https://skud-lite-backend.onrender.com
   ```
   (ваш URL из шага 3.6)
3. Можете выбрать цвет, добавить логотип
4. Нажмите **Create QR Code**
5. Нажмите **Download PNG**

### Шаг 6.2: Печать

1. Откройте скачанный файл
2. Распечатайте (чем крупнее, тем легче сканировать)
3. Заламинируйте если есть возможность
4. Повесьте на стене рядом со входом/выходом

---

## 7. Как пользоваться системой

### 👨‍💼 Для жильцов

**Первый раз (регистрация):**
1. Откройте камеру телефона
2. Наведите на QR-код
3. Нажмите на появившуюся ссылку
4. Введите своё ФИО (Фамилия Имя)
5. Нажмите "Войти"

**При уходе:**
1. Отсканируйте QR-код
2. Выберите причину:
   - 💼 **На работу** — уходите на работу
   - 🌙 **На сутки** — уходите на сутки
   - 📋 **По заявлению** — другая причина

**При возвращении:**
1. Отсканируйте QR-код
2. Нажмите зелёную кнопку **"Я вернулся"**

---

### 👮 Для дежурного (Telegram-бот)

Найдите бота в Telegram и используйте кнопки:

| Кнопка | Что делает |
|--------|------------|
| 📊 **Сводка** | Показывает: на месте 205, отсутствует 45 и т.д. |
| 📋 **Список отсутствующих** | Показывает ФИО всех отсутствующих с причинами |
| 📍 **Местоположение** | Отправляет точки на карте (если жилец разрешил GPS) |
| 🔄 **Сбросить статусы** | Возвращает ВСЕХ в статус "В здании" |

---

## ✅ Итоговая архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    ОБЛАКО (Render)                       │
│  ┌─────────────────────┐    ┌─────────────────────────┐ │
│  │    Backend + Web    │◄───│    Telegram Bot         │ │
│  │  (skud-lite-backend)│    │   (skud-lite-bot)       │ │
│  │                     │    │                         │ │
│  │  • API              │    │  • Команды /check и др. │ │
│  │  • Веб-страница     │    │  • Отправка сводок      │ │
│  │  • База данных      │    │  • Геолокация           │ │
│  └─────────────────────┘    └─────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
          ▲                              ▲
          │                              │
    Жильцы сканируют              Дежурный в Telegram
       QR-код
```

**Всё работает 24/7, ваш компьютер не нужен!**

---

## ❓ Частые проблемы

### Проблема: "Нет связи с сервером"
- **Причина:** Сервер "заснул" (бесплатный план)
- **Решение:** Подождите 30-60 секунд и обновите страницу

### Проблема: Бот не отвечает
- **Проверьте:** Статус `skud-lite-bot` на Render — должен быть **Live**
- **Проверьте:** Правильно ли указаны переменные окружения

### Проблема: "Доступ запрещён" в боте
- **Причина:** Ваш Telegram ID не добавлен
- **Решение:** Добавьте ID в переменную `ADMIN_IDS` на Render

### Проблема: Сервер постоянно засыпает
- **Решение:** Настройте бесплатный UptimeRobot:
  1. Зарегистрируйтесь на https://uptimerobot.com
  2. Создайте монитор типа HTTP(S)
  3. Введите URL вашего backend
  4. Интервал: 5 минут
  5. Это будет "будить" сервер каждые 5 минут

---

## � Как обновить систему

Если нужно внести изменения в код:

1. Измените файлы на GitHub
2. Render автоматически обнаружит изменения
3. Сервисы перезапустятся с новым кодом

---

## 📞 Контрольный чек-лист

После настройки проверьте:

- [ ] Backend открывается в браузере
- [ ] Можно зарегистрироваться как жилец
- [ ] Можно сменить статус
- [ ] Бот отвечает на /start
- [ ] Кнопка "Сводка" показывает статистику
- [ ] QR-код распечатан и висит на видном месте
