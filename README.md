# СКУД-лайт: Система учета присутствия жильцов

Простая система учета ~250 жильцов режимного объекта. Жильцы сканируют QR-код и отмечают свой статус через мобильный браузер. Дежурный получает сводку через Telegram-бота.

## ✨ Возможности

- 📱 Мобильный веб-интерфейс для жильцов
- 🤖 Telegram-бот для дежурного
- 📍 Геолокация (GPS) отсутствующих
- 📝 Логирование всех действий в файл
- 🚀 Единый запуск через uvicorn

## 📁 Структура проекта

```
tgbot ufitc/
├── backend/
│   ├── main.py           # FastAPI API + раздача frontend
│   ├── models.py         # Модели БД (с геолокацией)
│   ├── database.py       # Настройка SQLite
│   ├── logs/             # Логи активности
│   │   └── activity.log
│   └── requirements.txt
├── frontend/             # Раздаётся через FastAPI
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── bot/
│   ├── bot.py
│   ├── requirements.txt
│   └── .env.example
└── README.md
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Backend + Frontend (один процесс)
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt

# Telegram-бот (отдельный процесс)
cd ../bot
pip install -r requirements.txt
```

### 2. Запуск сервера (Backend + Frontend)

```bash
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

🌐 Откройте http://localhost:8000 — увидите веб-интерфейс для жильцов!

### 3. Настройка и запуск Telegram-бота

```bash
cd bot
cp .env.example .env
# Отредактируйте .env — добавьте BOT_TOKEN и ADMIN_IDS
python bot.py
```

---

## 📍 Геолокация

При смене статуса frontend запрашивает GPS (если пользователь разрешил). Координаты сохраняются в БД.

Дежурный может:
- Видеть 📍 в списке отсутствующих (у кого есть GPS)
- Нажать кнопку «Местоположение» — получить точки на карте

---

## 📝 Логирование

Все действия записываются в `backend/logs/activity.log`:

```
2026-01-06 10:30:15 | Иванов Иван | inside -> work | GPS: 55.751244, 37.618423
2026-01-06 11:45:22 | Петров Петр | work -> inside
2026-01-06 12:00:00 | ADMIN | Сброс всех статусов на 'inside'
```

---

## 🔧 API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| GET | / | Веб-интерфейс (index.html) |
| POST | /api/register | Регистрация жильца |
| GET | /api/status/{user_id} | Получить статус |
| POST | /api/status/{user_id} | Изменить статус (+ GPS) |
| GET | /api/stats | Статистика |
| GET | /api/absent | Список отсутствующих (+ GPS) |
| POST | /api/reset | Сбросить все статусы |

---

## 🤖 Команды бота

| Команда | Описание |
|---------|----------|
| /start | Приветствие и меню |
| /check | Сводка по личному составу |
| /absent | Список отсутствующих |
| /locations | 📍 Местоположение на карте |
| /reset | Сбросить все статусы |

---

## 💡 Деплой

1. Разверните backend на VPS (DigitalOcean, Hetzner)
2. Настройте Nginx как reverse proxy на порт 8000
3. Запустите бота через systemd
4. Создайте QR-код с URL сервера

---

## 📝 Лицензия

MIT License
